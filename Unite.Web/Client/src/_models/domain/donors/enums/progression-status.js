export default class ProgressionStatus {
    static values = {
        "Progression": true,
        "No Progression": false
    };

    static Yes = this.values["Progression"];
    static No = this.values["No Progression"];
}
