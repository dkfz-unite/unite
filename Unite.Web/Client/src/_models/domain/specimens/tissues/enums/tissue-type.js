export default class TissueType {
    static values = {
        "Control": "Control",
        "Tumor": "Tumor"
    };

    static Tumor = this.values["Tumor"];
    static Control = this.values["Control"];
}