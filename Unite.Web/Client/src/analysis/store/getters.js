const getters = {
  // namespace(state, getters, rootState, rootGetters) {
  //   const email = rootState.identity?.account?.email ?? "anonymous";
  //   return `${email}-analyses`;
  // }
};

export default getters;