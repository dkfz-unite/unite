export default class IdhStatus {
    static values = {
        "Wild Type": "Wild Type",
        "Mutant": "Mutant"
    };

    static WildType = this.values["Wild Type"];
    static Mutant = this.values["Mutant"];
}